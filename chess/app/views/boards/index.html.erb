<%= link_to "Create New Game", new_board_path %>

<!--<%= @boards.inspect %>-->

<table class="table_join">
	<tr>
		<td>id</td>
		<td>join table </td>
	</tr>
	<%= paginate @boards %>
	<%@boards.each do |p|%>
	<tr>
		<td><%=p.id%></td>
		<td><u><%= link_to "join", p %></u></td>
	</tr>	
	<%end%>
</table>

